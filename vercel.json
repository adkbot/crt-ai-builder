{
    "framework": "nextjs",
    "buildCommand": "npm run build",
    "installCommand": "npm ci",
    "outputDirectory": ".next",
    "cleanUrls": true,
    "env": {
        "OPENAI_API_KEY": "@openai_api_key"
    },
    "regions": [
        "gru1"
    ],
    "functions": {
        "app/api/**/*.ts": {
            "maxDuration": 300
        }
    }
}